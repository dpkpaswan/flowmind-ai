"""FlowMind AI -- Middleware Package.

Contains production middleware:

    * ``security`` -- OWASP security headers, per-IP rate limiting
      (token-bucket), input sanitization, and ETag generation.
"""
